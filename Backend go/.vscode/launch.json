{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Executar API Komercio",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/cmd/main.go",
            "env": {
                "GIN_MODE": "debug",
                "PORT": "8000",
                "DATABASE_URL": "postgres://komercio:komercio@localhost:5432/komercio?sslmode=disable"
            },
            "args": [],
            "cwd": "${workspaceFolder}"
        }
    ]
}